query customer($where: CustomerWhereInput!) {
  customer(where: $where) {
    customerId
    userId
    defaultCity
    defaultAddress
    marketingOptIn
    user {
      userId
      email
      name
      phone
      country
    }
  }
}
