query login($data: LoginUserInput!){#TODO change the login query to return all the values possible/needed
  login(data: $data){
    access_token,
    expiresAt,
    user{
      userId
      email
      language
    }
  }
}