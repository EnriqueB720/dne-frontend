mutation socialLogin($data: SocialLoginInput!) {
  socialLogin(data: $data) {
    access_token
    expiresAt
    user {
      userId
      email
      name
      isCustomer
      isSupplier
    }
  }
}
