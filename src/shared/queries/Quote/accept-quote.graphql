mutation acceptQuote($data: QuoteAcceptInput!) {
  acceptQuote(data: $data) {
    bookingId
    requestId
    quoteId
    customerId
    supplierId
    serviceDate
    location
    guestCount
    totalPrice
    platformFee
    supplierPayout
    currency
    status
    paymentStatus
    createdAt
  }
}
