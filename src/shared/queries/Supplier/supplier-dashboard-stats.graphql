query supplierDashboardStats($supplierId: Int!) {
  supplierDashboardStats(supplierId: $supplierId) {
    responseRate
    conversionRate
    activeLeadsCount
    mtdEarnings
    mtdGross
    currency
    platformFeeRate
    weeklyLeadCounts
  }
}
