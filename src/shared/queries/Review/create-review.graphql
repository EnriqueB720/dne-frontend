mutation createReview($data: ReviewCreateInput!) {
  createReview(data: $data) {
    reviewId
    bookingId
    customerId
    supplierId
    rating
    text
    ratingQuality
    ratingCommunication
    ratingValue
    ratingPunctuality
    createdAt
  }
}
